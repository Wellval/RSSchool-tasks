import './select-setting.scss';
import { BaseComponent } from '../base-component';

export class SelectSetting extends BaseComponent {
    constructor() {
        super('select', ['select-setting']);
    }
}
